{#
/**
* @file
* Theme override for a three-column layout.
*
* Available variables:
* - content: The content for this layout.
* - attributes: HTML attributes for the layout <div>.
  */
  #}
    {%
  set classes = [
  'layout',
  'layout--threecol',
  ]
  %}
  {% if content %}
    <div{{ attributes.addClass(classes) }}>
      <div class="container">
        <div class="row">
          {% if content.first %}
            <div {{ region_attributes.first.addClass('layout__region--first col-lg-4') }}>
              {{ content.first }}
            </div>
          {% endif %}
          {% if content.second %}
            <div {{ region_attributes.second.addClass('layout__region--second col-lg-4') }}>
              {{ content.second }}
            </div>
          {% endif %}
          {% if content.third %}
            <div {{ region_attributes.third.addClass('layout__region--third col-lg-4') }}>
              {{ content.third }}
            </div>
          {% endif %}
        </div>
      </div>
    </div>
  {% endif %}